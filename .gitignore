# ── Dependencies ──
node_modules/
.pnp
.pnp.js
package-lock.json
yarn.lock
pnpm-lock.yaml

# But keep semantic-grep lock for reproducibility (un-ignore)
!semantic-grep/package-lock.json
!semantic-grep/yarn.lock

# ── Pi local state (596M vendor, cache) ──
.pi/
model-cache/
semantic-grep/model-cache/
semantic-grep/node_modules/
semantic-grep/dist/
semantic-grep/build/

# ── Build outputs ──
dist/
build/
.next/
out/
coverage/
.nyc_output/
*.tsbuildinfo

# ── Env & secrets ──
.env
.env.*
!.env.example
*.pem
*.key
secrets/

# ── Logs ──
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# ── OS ──
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
desktop.ini

# ── Editor ──
.vscode/
.idea/
*.swp
*.swo
*~
.project
.classpath

# ── Temporary ──
.tmp/
temp/
tmp/
*.tmp
*.temp

# ── GitHub ──
# keep .github/ if you add workflows

# ── Tests artifacts ──
junit.xml
test-results/
playwright-report/

# ── Sharp / native vendor (huge) ──
sharp/
vendor/
