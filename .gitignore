# ── Dependencies ──
node_modules/
.pnp
.pnp.js
pnpm-lock.yaml
# package-lock.json and yarn.lock intentionally NOT ignored — keep lock for reproducibility

# ── Pi local state (596M vendor, cache) ──
.pi/
model-cache/

# ── Build outputs ──
dist/
build/
.next/
out/
coverage/
.nyc_output/
*.tsbuildinfo

# ── Env & secrets ──
.env
.env.*
!.env.example
*.pem
*.key
secrets/

# ── Logs ──
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# ── OS ──
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
desktop.ini

# ── Editor ──
.vscode/
.idea/
*.swp
*.swo
*~
.project
.classpath

# ── Temporary ──
.tmp/
temp/
tmp/
*.tmp
*.temp

# ── GitHub ──
# keep .github/ if you add workflows

# ── Tests artifacts ──
junit.xml
test-results/
playwright-report/

# ── Sharp / native vendor (huge) ──
sharp/
vendor/
