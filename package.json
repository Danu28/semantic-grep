{
  "name": "semantic-grep",
  "version": "0.1.0",
  "description": "Pi extension: semantic code search via local neural embeddings (384-dim, 22MB MiniLM) — grep by meaning",
  "type": "module",
  "pi": {
    "extensions": ["./index.ts"]
  },
  "dependencies": {},
  "scripts": {
    "test": "node tests/test-simple.mjs",
    "test:pi": "pi -e ./index.ts --mode print -p \"hello\""
  },
  "keywords": ["pi", "pi-extension", "semantic-search", "embeddings", "neural-network"],
  "author": "",
  "license": "MIT"
}
